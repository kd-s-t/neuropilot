# Headband to drone flow

Muse 2 → muselsl → pylsl (LSL stream) → band powers → machine by user id → machine controls / bindings → pattern matching → accumulate 5 actions → YOLO on camera frame → OpenAI decide → send commands → Tello
